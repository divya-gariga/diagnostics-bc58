server:
  port: 8085
spring:
  application:
    name: USERS-SERVICE
eureka:
  client:
    register-with-client: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

